<h1>Tasks#index</h1>
<p>Find me in app/views/tasks/index.html.erb</p>

<% @tasks.each do |t| %>
  <% t.title %>
  <% t.description %>
  <%= link_to "Delete", user_task_path(current_user, t), method: :delete %>
<% end %>

<%= link_to " New Task", new_user_task_path(current_user, @task) %>
<%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
